import 'package:flutter/material.dart';

class SessionsSettings extends StatelessWidget {
  const SessionsSettings({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('SessionsSettings'),
    );
  }
}