import 'package:flutter/material.dart';

class CallsBar extends StatelessWidget {
  const CallsBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('CallsBar'),
    );
  }
}
