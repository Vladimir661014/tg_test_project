import 'package:flutter/material.dart';

class StickersSettings extends StatelessWidget {
  const StickersSettings({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('StickersSettings'),
    );
  }
}