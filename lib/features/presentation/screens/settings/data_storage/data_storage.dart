import 'package:flutter/material.dart';

class DataStorage extends StatelessWidget {
  const DataStorage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DataStorage'),
    );
  }
}