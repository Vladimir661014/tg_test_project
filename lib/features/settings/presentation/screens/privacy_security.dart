import 'package:flutter/material.dart';

class PrivacyAndCecurity extends StatelessWidget {
  const PrivacyAndCecurity({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('PrivacyAndCecurity'),
    );
  }
}